spring:
  application:
    name: tsinjo-std23016

  datasource:
    username: postgres
    password: liantsoa2
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration

  thymeleaf:
    cache: false
