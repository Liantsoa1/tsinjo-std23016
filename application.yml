spring:
  application:
    name: tsinjo-std21001

  datasource:
    username: postgres
    password: liantsoa2
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration

  thymeleaf:
    cache: false
